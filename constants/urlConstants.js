const { config: { BASE_URL } } = require('../config/config');

////////////////////////////////Constants///////////////////////////////////////////////////
export const RESERVATION = `${BASE_URL}reservation`;
